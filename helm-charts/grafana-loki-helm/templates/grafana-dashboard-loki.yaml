apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana-loki-stack.fullname" . }}-loki-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  loki-pods-dashboard.json: |
{{ .Files.Get "files/dashboards/loki-pods-dashboard.json" | indent 4 }}
